extends ../base

block content 
    if errorMessage
    form(method="POST")
        input(name="oldPassword", type="password", placeholder="oldPassword", required)
        input(name="newPassword", type="password", placeholder="newPassword", required)
        input(name="newPasswordConfirm", type="password", placeholder="newPasswordComfirm", required)
        input(value="Change Password" type="submit")